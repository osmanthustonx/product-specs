# Proposal: TodoList CRUD

## Motivation
展示 Spec-Driven Development Workflow 的完整流程。

## Problem Statement
需要一個具體功能來驗證從 PM spec 到工程實作的端到端流程。

## Expected Outcome
可運作的 TodoList 應用，完整走過 spec-driven workflow。

## Initial Scope
基本 CRUD + 篩選 + in-memory 儲存。
